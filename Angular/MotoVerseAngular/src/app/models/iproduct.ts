export interface IProduct {
    id: number,
    name: string,
    category: string,
    price: number,
    stock: number,
    title: string,
    image: string,
    exclusive: boolean,
    rating:number 
}
